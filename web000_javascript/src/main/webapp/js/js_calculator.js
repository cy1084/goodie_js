/**
 * 
 */

/*
let inputValue='';
let value = 0;
let op='';

window.onload = function() {	
	var tds = document.getElementsByTagName("td");
	for (let i = 0; i < tds.length; i++) {
		tds[i].onclick = function() {
			console.log(this.textContent);
		}
	}
}

const operatorCheck = /[+\-*\/]$/;

function calculate(operatorCheck) {
	value = Number(value);
	inputValue = Number(inputValue);

	switch (operatorCheck) {
		case '/':
			value /= inputValue;
			console.log(value);
			break;
		case '*':
			value *= inputValue;
			break;
		case '-':
			value -= inputValue;
			break;
		case '+':
			value += inputValue;
			break;
	}
	inputValue=value;
}
*/






